<h1>Login</h1>

<%= form_with(url: login_path, method: :post, local: true) do |form| %>
  <div>
    <%= form.label :username %>
    <%= form.text_field :username, required: true %>
  </div>

  <%= form.submit "Log In" %>
<% end %>
